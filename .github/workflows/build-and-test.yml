name: Build and Test

on:
  push:
    branches: [ main, master, '**' ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies & Wine
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y wine64 wine32 wget xvfb

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Install Poetry Dependencies
        run: |
          poetry install

      - name: Setup Wine Python (setup-wine-python)
        env:
          WINEDEBUG: -all
        run: |
          xvfb-run -a poetry run poe setup-wine-python

      - name: Run Unit Tests under Wine (test)
        env:
          WINEDEBUG: -all
          QT_QPA_PLATFORM: offscreen
        run: |
          xvfb-run -a poetry run poe test

      - name: Build Windows Executable under Wine (build-windows-wine)
        env:
          WINEDEBUG: -all
        run: |
          xvfb-run -a poetry run poe build-windows-wine

      - name: Upload Windows Executable Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SFTPMounter-Windows-Executable
          path: dist/*.exe
          if-no-files-found: warn
